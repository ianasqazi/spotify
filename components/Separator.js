// Separator.js . It's built for you.

import React, { Component } from 'react';
import { View } from 'react-native';

export default () => (
    <View
        style={{
            height: 1,
            backgroundColor: '#BFA18F',
            marginTop: 10,
            marginBottom: 10,
        }}
    />
);